import { Injectable } from "@angular/core";

@Injectable({
  providedIn: "root",
})
export class GlobalService {
  /*   private userCredentials: BehaviorSubject<IUserCredentials> =
    new BehaviorSubject(userCredentials);
  userCredentials$ = this.userCredentials.asObservable(); */
  constructor() {}
}
